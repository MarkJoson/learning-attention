# 核心依赖（本仓库在 RTX 4090 + CUDA 12 + PyTorch 2.x 上开发验证）
torch>=2.4
triton>=3.0
einops>=0.8
einx>=0.3            # 08 NSA 深度优化版 kernel（nsa_triton.py）依赖

# 测试与基准
pytest>=8.0
numpy

# notebook 呈现与可视化
jupyter
matplotlib>=3.7

# 可选：作为对照 baseline 的官方 CUDA 实现，编译较慢，不装也能跑通本仓库
# flash-attn>=2.5

# 可选：08 NSA 深度优化版的输入准备与测试对照（提供 SparseAttention Module；nsa_triton.py 本身不需要它）
# native-sparse-attention-pytorch==0.2.3

# 可选：07 MoBA 高效版对照（moba_efficient.py，仅阅读；moba_naive.py 无需任何额外依赖）
# flash-attn==2.6.3

# 可选：10 GLA 深度优化版的忠实性测试对照（解耦后的本地 kernel 本身不依赖 fla）
# pip install --no-deps flash-linear-attention   # 务必 --no-deps，避免改动 torch/triton 版本
