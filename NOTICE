learning-attention —— 第三方来源与 License 汇总
================================================

本仓库以"学习"为目的，精读并拷贝了多个开源项目的 Triton/CUDA kernel 实现。
所有拷贝来的代码均：
  1. 在文件头保留/标注原始来源（仓库、文件路径、commit）与原始 license；
  2. 在对应变体目录的 SOURCES.md 中登记；
  3. 在本文件汇总。

自写部分（参考实现、测试、基准、文档、notebook）默认采用 MIT License。
拷贝部分的版权与 license 归原作者所有，使用时请遵循其原始条款。

--------------------------------------------------------------------------------
已登记来源
--------------------------------------------------------------------------------

[1] triton-lang/triton
    用途   : FlashAttention v2 的 Triton kernel
    文件   : python/tutorials/06-fused-attention.py
    tag    : v3.1.0 / cf34004b8a67d290a962da166f5aa2fc66751326
    license: MIT (Copyright 2018-2020 Philippe Tillet; 2020-2022 OpenAI)
    用于   : 02-flash-attention/
    说明   : Tri Dao 的 FlashAttention v2 算法，OpenAI kernel team 编写。选 v3.1.0
             经典版（非最新 TMA 版，4090 无法运行），仅调 autotune 参数适配 RTX 4090，
             计算逻辑未改。

[2] vllm-project/vllm
    用途   : 原生 GQA/MQA 的 prefill attention kernel（含 sliding window、varlen）
    文件   : vllm/v1/attention/ops/triton_prefill_attention.py
    commit : 435f82d61a1eddb84854ca59a008a8e4d97ab439
    license: Apache-2.0（原始来源链 LightLLM → SGLang → vLLM，文件内保留其 license 头）
    用于   : 03-gqa-mqa/、04-sliding-window/（复用同一 kernel）
    说明   : 仅去除 3 个 vLLM 内部依赖（current_platform / triton_utils / RCP_LN2），
             kernel 计算逻辑一字未改。

[3] ModelTC/lightllm（两个 kernel，license Apache-2.0，commit 9ae6a5d4886312bd827295f3cb0de231639f0c77）
    (a) paged decode attention（block table 间接寻址）
        文件: lightllm/common/basemodel/triton_kernel/att/decode_att/gqa/
              gqa_decode_flashattention_nopad.py
        用于: 05-paged-attention/；依赖本就干净（torch/triton/math），原样拷贝。
    (b) MLA prefill attention（deepseek2，absorb 格式，latent 维度计算）
        文件: lightllm/models/deepseek2/triton_kernel/context_flashattention_nopad.py
        用于: 06-mla/；去除 1 个 is_tesla 依赖 + 针对 4090 收紧 BLOCK，计算逻辑未改。

[4] lucidrains/native-sparse-attention-pytorch
    用途   : NSA（Native Sparse Attention）的稀疏 flash kernel（selected + sliding，含 backward、GQA）
    文件   : native_sparse_attention_pytorch/triton_native_sparse_attention.py
    版本   : pip native-sparse-attention-pytorch==0.2.3
    license: MIT (Copyright Phil Wang / lucidrains)
    用于   : 08-native-sparse-attention/
    说明   : 完整提取整份 1987 行 kernel，仅去除 1 个 tensor_typing（jaxtyping 注解）依赖，
             计算逻辑一字未改。kernel 谱系改编自 Dao-AILab/flash-attention 的 triton flash。

[5] MoonshotAI/MoBA（Kimi Mixture of Block Attention）
    用途   : 动态块稀疏（chunk 级 top-k 选块 + 当前块必选）的对照实现
    文件   : moba/moba_naive.py（纯 PyTorch 参考，可跑）、moba/moba_efficient.py（flash_attn 编排，仅阅读）
    commit : b5d58363311d3ca946f1ec444182727c15e338b5
    license: MIT (Copyright © 2025 Moonshot AI)
    用于   : 07-block-sparse-attention/（NSA 之外另一条动态块稀疏路线的对照）
    说明   : naive 版完整拷贝、计算逻辑未改；efficient 版依赖 flash-attn==2.6.3（本环境未装），仅供阅读。
             07 深度优化版本身复用 08 的 NSA kernel（见 [4]），未引入新 kernel。

[6] fla-org/flash-linear-attention
    用途   : 线性注意力线的 chunk-parallel triton kernel —— GLA（10）+ DeltaNet（11，delta rule/WY）+ KDA（12，
             gated delta）+ GDN/GDN-2（13，门控 delta，Qwen3-Next/3.5）+ Mamba2 SSD（15，simple_gla = 标量衰减 GLA）
    文件   : ops/gla/chunk.py + ops/common/chunk_h.py + ops/utils/cumsum.py（解耦到本地三件套）；
             ops/gated_delta_rule/* + ops/gdn2/* + ops/kda/*（13 章复用）；ops/simple_gla/* + ops/common/chunk_o.py
             （15 章 Mamba2 SSD，详见各章 SOURCES.md）
    commit : 0b27f7b
    license: MIT (Copyright fla-org)
    用于   : 10-linear-attention/（GLA，3 文件）、11-deltanet/（DeltaNet，8 文件）、12-kda/（KDA，14 文件）、
             13-gated-deltanet/（GDN 9 文件 + GDN-2 13 文件）、15-mamba/（Mamba2 SSD = simple_gla，4 文件）；
             均 no-op dispatch（13 另加 cp stub + 跨章复用）收敛闭包脱离 fla
    说明   : 完整拷贝核心 kernel（计算逻辑一字未改），仅把对 fla 框架的 import 改指向本地薄适配层
             _fla_*_compat.py（复现 exp2/autotune/check_shared_mem/input_guard 等），脱离 fla 包独立运行；
             定长与变长（cu_seqlens）序列均支持。各章 test_*_vs_fla 验证与 fla 原版近 bitwise 一致（含变长）。

[7] DeepSeek V4 Hybrid Attention（CSA + HCA）—— 仅指向来源，未提取 kernel
    用途   : 长上下文混合压缩稀疏注意力机制（CSA 压缩+稀疏选块 / HCA 极致压缩+稠密）
    来源   : DeepSeek-V4 技术报告（deepseek-ai/DeepSeek-V4-Pro, HuggingFace, 2026-04）；
             vLLM DeepSeek-V4 实现（FP4 indexer / FP8 core / KV cache 布局）；
             NVIDIA Blackwell 部署博客《Build with DeepSeek V4》
    license: 见各来源（DeepSeek 模型许可 / vLLM Apache-2.0）
    用于   : 14-deepseek-v4/（自写简要版讲机制 + 指向来源）
    说明   : 按用户约定本章**不提取**生产 kernel —— DSv4 不在 fla，生产实现在 vLLM/官方。14-deepseek-v4/
             deepseek_v4.py 是**自写的纯 PyTorch 机制简化版**（compress→index→attend 主干），刻意省略 FP4/FP8
             量化、dual-stream 重叠压缩、KV cache 工程等生产细节；详见 14-deepseek-v4/SOURCES.md。

--------------------------------------------------------------------------------
待登记（随变体推进补充）
--------------------------------------------------------------------------------

- epfml/dynamic-sparse-flash-attention       —— DSA（稀疏线）
- flashinfer-ai/flashinfer         —— 推理 attention kernel 接口
- Dao-AILab/flash-attention        —— 工程接口参考
